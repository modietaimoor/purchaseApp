import { ButtonIconDirective } from './button-icon.directive';

xdescribe('ButtonIconDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ButtonIconDirective();
  //   expect(directive).toBeTruthy();
  // });
});
